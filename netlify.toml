[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — todas as rotas caem no index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
